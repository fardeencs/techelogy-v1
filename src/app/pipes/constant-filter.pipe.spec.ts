import { ConstantFilterPipe } from './constant-filter.pipe';

describe('ConstantFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConstantFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
